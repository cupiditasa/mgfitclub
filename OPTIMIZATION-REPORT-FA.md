# گزارش بهینه‌سازی نسخه 20260813.8

تاریخ تست: ۱۳ اوت ۲۰۲۶

## تغییرات

- ۴۵ تصویر بخش `assets/images` با نسخه WebP جایگزین شد.
- فایل تکراری `gym-22-1.webp` وارد پروژه نشد.
- تمام مسیرهای ثابت و پویای تصاویر در `index.html` و `app.js` به WebP تغییر کرد.
- نسخه کش فایل‌های رابط کاربری از `20260813.6` به `20260813.8` تغییر کرد.
- دو ویدئوی تعاملی با H.264، رزولوشن و نرخ ۲۴ فریم اصلی بهینه شدند.
- فاصله Keyframe هر دو ویدئو روی ۰٫۲۵ ثانیه حفظ شد تا حرکت و Scrub با موس روان باقی بماند.
- طراحی، CSS، کد ربات، Shadow DOM و منطق اکشن‌های سایت تغییر نکرده‌اند.

## کاهش حجم

- تصاویر: از 14,202,981 به 6,673,016 بایت — کاهش 53.02٪
- ویدئوها: از 9,600,381 به 8,084,088 بایت — کاهش 15.79٪
- کل پروژه: از 23,939,920 به حدود 14,893,790 بایت — کاهش 37.79٪

## نتیجه تست

- هر ۴۵ فایل WebP از سرور محلی با وضعیت HTTP 200 و MIME صحیح بارگذاری شد.
- ۴۷ مسیر ثابت و تولیدشونده تصاویر/ویدئو بررسی شد و مسیر گمشده وجود نداشت.
- ابعاد هر ۴۵ تصویر WebP با فایل قبلی مطابقت دارد.
- هر دو ویدئو Range Request را با HTTP 206 پاسخ دادند.
- Hero: ابعاد 1440×960، نرخ 24fps و ۲۱ Keyframe حفظ شد.
- Club Live: ابعاد 900×1350، نرخ 24fps و ۴۱ Keyframe حفظ شد.
- مقایسه کیفیت ویدئوها: SSIM بین 0.9899 و 0.9941 و PSNR بین 45.08 و 47.25 دسی‌بل.
- مقایسه بصری فریم‌های قبل و بعد، افت محسوس نشان نداد.
- API محلی ربات به سرویس `chat.mgfitclub.ir` متصل شد و پاسخ واقعی فارسی با HTTP 200 دریافت شد.
- Site key فعال: `site_-ejTQPH6RC91X28aTTwxBL3A`

