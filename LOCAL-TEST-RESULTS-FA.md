# نتیجه تست محلی نسخه 20260813.6

تست در Microsoft Edge و روی سرور محلی `127.0.0.1:8765` انجام شد.

## نتایج موفق

- بسته‌شدن Preloader و اجرای بدون خطای JavaScript
- بارگذاری کامل و پخش خودکار ویدئوی Hero
- فعال‌شدن Scrub ویدئو با حرکت موس
- توقف Scrub و ادامه پخش پس از خروج موس
- چرخش دو حلقه ربات در جهت مخالف هنگام Hover
- اکشن لمس طولانی ربات روی نمایشگر موبایل
- باز و بسته‌شدن پنل ربات
- ارسال پیام و دریافت پاسخ واقعی از API
- عملکرد صحیح اسکرول، Header، Revealها و منوی موبایل
- نمایش صحیح در اندازه موبایل 390×844

## علت مشکل اکشن‌ها

مرورگر سیستم `prefers-reduced-motion: reduce` گزارش می‌داد. نسخه جدید فقط اکشن ضروری ویدئوی اول و Hover ربات را فعال نگه می‌دارد؛ حرکت‌های سنگین‌تر سایت برای جلوگیری از کندی به این تنظیم احترام می‌گذارند.

## علت پاسخ‌ندادن در تست لوکال

Windows PowerShell قدیمی، متن JSON فارسی را هنگام عبور از ورودی و خروجی کنسول با encoding نادرست منتقل می‌کرد. Proxy محلی نسخه 20260813.6 بدنه درخواست و پاسخ را به‌صورت UTF-8 باینری منتقل می‌کند.

تست نهایی با خود فایل اجرای ویندوز:

- وضعیت صفحه: 200
- وضعیت API: 200
- نمایش متن فارسی: صحیح
- پاسخ در رابط ربات: صحیح
- خطای JavaScript: صفر

## بهینه‌سازی سرعت

- ویدئوی دوم و حرکت‌های سه‌بعدی سنگین در سیستم‌هایی که کاهش حرکت فعال دارند اجرا نمی‌شوند.
- اکشن موس ویدئوی اول حفظ شده است.
- فاصله Seek ویدئوی اول از 80 به 140 میلی‌ثانیه افزایش یافته است.
- آستانه تغییر زمان ویدئو افزایش یافته تا Seekهای بسیار کوچک و پرهزینه حذف شوند.
- فعالیت پردازشی صفحه در تست بیکار دوثانیه‌ای حدود 2.2 میلی‌ثانیه بود و Layout/Style مداوم ثبت نشد.

## اجرای تست توسط کاربر

فایل `START-LOCAL-TEST.bat` را اجرا کنید. مرورگر معمولاً روی آدرس زیر باز می‌شود:

```text
http://127.0.0.1:8765/
```

اگر پورت 8765 اشغال باشد، اجراکننده به‌صورت خودکار یکی از پورت‌های 8766 تا 8785 را انتخاب می‌کند و آدرس دقیق را داخل پنجره مشکی نشان می‌دهد.

پنجره تست را تا پایان باز نگه دارید و برای توقف `Ctrl+C` بزنید. اجرای هم‌زمان چند نسخه لازم نیست.
